name: Daily outreach cron

# Vercel's own Cron Jobs feature never registered these routes (confirmed
# via Observability > Cron Jobs showing zero invocations), so GitHub Actions
# triggers them instead — same endpoints, same CRON_SECRET auth, just a
# different, verifiable scheduler.
on:
  schedule:
    # Campaign sends are paused (2026-08-14) while the send pipeline is
    # reviewed — see campaign-send below, now workflow_dispatch-only.
    - cron: "*/15 * * * *"  # every 15 min — personal inbox check (replaces the standalone Inbox page)
    - cron: "0 19 * * *"  # 7am NZT — morning brief (today's meetings + due items, via Slack)
    - cron: "0 21 * * *"  # 9am NZT — daily maintenance (replies, stale-reply pings, etc.)
  workflow_dispatch: {}    # lets you trigger a run manually from the Actions tab

jobs:
  campaign-send:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Call /api/cron
        run: |
          curl -sf -X GET "https://app.lsgrowth.agency/api/cron" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"

  check-personal-inbox:
    if: github.event.schedule == '*/15 * * * *' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Call /api/cron/check-personal-inbox
        run: |
          curl -sf -X GET "https://app.lsgrowth.agency/api/cron/check-personal-inbox" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"

  morning-brief:
    # Not gated on github.event.schedule: when two schedules in this workflow
    # coincide (0 19 * * * always lands on a */15 boundary), GitHub Actions
    # fires a single run and event.schedule reflects only one of them - in
    # practice always '*/15 * * * *', so an event.schedule match here never
    # fired and this job silently no-op'd every day. Check wall-clock time
    # instead, which is robust to the collision.
    runs-on: ubuntu-latest
    steps:
      - name: Check if it's the 7am NZT (19:00 UTC) window
        id: check
        run: |
          HOUR=$(date -u +%H)
          MIN=$(date -u +%M)
          if [ "$HOUR" = "19" ] && [ "$MIN" -lt 15 ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Call /api/cron/morning-brief
        if: steps.check.outputs.run == 'true' || github.event_name == 'workflow_dispatch'
        run: |
          curl -sf -X GET "https://app.lsgrowth.agency/api/cron/morning-brief" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"

  daily-maintenance:
    # Same event.schedule collision as morning-brief above (0 21 * * * also
    # lands on a */15 boundary) - check wall-clock time instead.
    runs-on: ubuntu-latest
    steps:
      - name: Check if it's the 9am NZT (21:00 UTC) window
        id: check
        run: |
          HOUR=$(date -u +%H)
          MIN=$(date -u +%M)
          if [ "$HOUR" = "21" ] && [ "$MIN" -lt 15 ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Call /api/cron/daily-maintenance
        if: steps.check.outputs.run == 'true' || github.event_name == 'workflow_dispatch'
        run: |
          curl -sf -X GET "https://app.lsgrowth.agency/api/cron/daily-maintenance" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
